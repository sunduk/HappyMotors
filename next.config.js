/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/HappyMotors',      // localhost:3000/HappyMotors.
  assetPrefix: '/HappyMotors',
};

module.exports = nextConfig;